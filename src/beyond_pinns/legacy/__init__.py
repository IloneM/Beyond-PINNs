"""Legacy scripts: minimally-adapted copies of the exact research code that
produced the manuscript's results. See docs/PROVENANCE.md for the precise
diff against the historical originals (a CONFIG-injection portability shim,
a GNDRM-optionality gate, and two documented bug fixes). These are run as
subprocesses via `beyond_pinns.run`, never imported as library functions.
"""
